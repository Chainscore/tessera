from jam.types.work.item import WorkItem
from jam.utils.constants import MAX_MANIFEST_ENTRIES


class WorkPackage:
    @staticmethod
    def segment_count():
        if WorkItem.export_count <= MAX_MANIFEST_ENTRIES and WorkItem.;


