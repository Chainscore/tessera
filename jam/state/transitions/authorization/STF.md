# Authorization State Transitions

State Transition for of a block involves:
- Modifying the α by placing a new authorization into the pool from the Queue and removing the oldest authorization from the pool.

Add new items to Queue from the accumulation. To be handled by Accumulate STF.
For this reason NOTE: Authorization STF should be handled after Accumulate STF

